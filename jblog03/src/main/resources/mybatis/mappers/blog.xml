<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">
	<insert id="insert" parameterType="String">
		<![CDATA[
			insert into blog(blog_id, title, profile)
			values (#{blogId}, "블로그 이야기", "/assets/upload-images/spring-logo.jpg")
		]]>				
	</insert>
	
	<update id="update" parameterType="blogvo">
		<![CDATA[
			update blog
			set title=#{title}, profile=#{profile}
			where blog_id = #{blogId}
		]]>					
	</update>
	
	<resultMap id="blogResultMap" type="blogvo">
	    <result property="blogId" column="blog_id" />
	    <result property="title" column="title" />
	    <result property="profile" column="profile" />
	</resultMap>

	<select id="findByBlogId" parameterType="String" resultMap="blogResultMap">
		<![CDATA[
			select blog_id, title, profile
			from blog
			where blog_id=#{blogId}
		]]>		
	</select>
	
</mapper>
